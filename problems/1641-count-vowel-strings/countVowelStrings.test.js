/**
 * @param {number} n
 * @return {number}
 */
const countVowelStrings = function (n) {
    const vowels = ['a', 'e', 'i', 'o', 'u'];
    const result = [];
    const curr = 0;
    for (let i = 0; i < vowels.length; i += 1) {
        for (let j = 1; j < vowels.length; j += 1) {

        }
    }
};

module.exports = { countVowelStrings };
